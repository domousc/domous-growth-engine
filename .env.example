# URL de webhook que recebe os leads do formulário em POST JSON
# (Make, Zapier, n8n, Google Apps Script ou o Domous CRM).
# Sem essa variável o formulário funciona normalmente, mas o lead
# só chega se a pessoa concluir o envio no WhatsApp.
VITE_LEAD_WEBHOOK_URL=
